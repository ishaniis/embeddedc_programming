#include <stdio.h>

int main(){
    int a, b, sum ;

    printf("Enter the value of the first number: \t");
    scanf("%d", &a);

    printf("Enter the second value: \t");
    scanf("%d",&b);

    sum = a +b ;
    printf("Sum of integer is: %d \n", sum);
    

    return 0;
   
}